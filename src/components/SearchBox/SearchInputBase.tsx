import { InputBase, styled } from "@mui/material";

export const StyledSearchInputBase = styled(InputBase)({
  marginLeft: 10,
  flex: 1,
  color: "hsla(0, 100%, 100%, 0.88)",
  fontSize: "16px",
  // TODO: Fix placeholder color in theme
});
