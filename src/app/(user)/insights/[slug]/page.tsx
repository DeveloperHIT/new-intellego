// import InsightView from "@/sections/view/insightView";

type Props = {
  params: { slug: string };
};

export default function SingleInsightPage(props: Props) {
  // return <InsightView {...props} />;
  return <div>Single Insight Page</div>;
}
